{{- if .Values.cloudiap.enabled }}
apiVersion: v1
data:
  client_id: {{ required "A valid .Values.cloudiap.clientId entry is required!" .Values.cloudiap.clientId }}
  client_secret: {{ required "A valid .Values.cloudiap.clientSecret is required!" .Values.cloudiap.clientSecret }}
kind: Secret
metadata:
  name: airflow-iap
{{- end }}
